export class Pc {
  constructor(public brand: string, public model: string, public screenSize: number, public processor: string, public specs: string, public screenType: string, public price: number, public source: string) {};
}
