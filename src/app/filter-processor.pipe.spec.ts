import { FilterProcessorPipe } from './filter-processor.pipe';

describe('FilterProcessorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProcessorPipe();
    expect(pipe).toBeTruthy();
  });
});
