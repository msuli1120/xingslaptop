import { FilterScreenTypePipe } from './filter-screen-type.pipe';

describe('FilterScreenTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterScreenTypePipe();
    expect(pipe).toBeTruthy();
  });
});
